# Sample Python CLI

A toy CLI used as a fixture for static analyzer tests.

## How to run

```bash
pip install -r requirements.txt
python -m src.main --name world
```

## Usage

```
python -m src.main --name <name>
```
